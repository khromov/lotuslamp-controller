name: LotusLamp
options:
  bundleIdPrefix: com.lotuslamp
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15"
  createIntermediateGroups: true

configs:
  Debug: debug
  Release: release

packages:
  ArgumentParser:
    url: https://github.com/apple/swift-argument-parser
    from: "1.3.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
  configs:
    Release:
      SWIFT_OPTIMIZATION_LEVEL: "-O"
      DEPLOYMENT_POSTPROCESSING: "YES"
      STRIP_INSTALLED_PRODUCT: "YES"
      COPY_PHASE_STRIP: "YES"

targets:
  LotusLamp:
    type: application
    platform: macOS
    sources:
      - path: Sources
        excludes:
          - Resources/Info.plist
          - Resources/LotusLamp.entitlements
    dependencies:
      - target: lotuslamp-cli
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lotuslamp.controller
        INFOPLIST_FILE: Sources/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Sources/Resources/LotusLamp.entitlements
        CODE_SIGN_IDENTITY: "Developer ID Application"
        DEVELOPMENT_TEAM: "36S2252ZTN"
        CODE_SIGNING_REQUIRED: "YES"
        ENABLE_HARDENED_RUNTIME: "YES"
      configs:
        Debug:
          CODE_SIGN_IDENTITY: "-"
          DEVELOPMENT_TEAM: ""
          CODE_SIGNING_REQUIRED: "NO"
          ENABLE_HARDENED_RUNTIME: "NO"
    postCompileScripts:
      - script: |
          cp "${BUILT_PRODUCTS_DIR}/lotuslamp" "${BUILT_PRODUCTS_DIR}/${PRODUCT_NAME}.app/Contents/Resources/lotuslamp"
        name: "Embed CLI Binary"
        basedOnDependencyAnalysis: false

  lotuslamp-cli:
    type: tool
    platform: macOS
    sources:
      - path: CLI
      - path: Sources/LampCommand.swift
        type: file
      - path: Sources/BLEConstants.swift
        type: file
      - path: Sources/PresetColor.swift
        type: file
      - path: Sources/EffectMode.swift
        type: file
    dependencies:
      - package: ArgumentParser
        product: ArgumentParser
    settings:
      base:
        PRODUCT_NAME: lotuslamp
        PRODUCT_BUNDLE_IDENTIFIER: com.lotuslamp.cli
        CODE_SIGN_IDENTITY: "Developer ID Application"
        DEVELOPMENT_TEAM: "36S2252ZTN"
        CODE_SIGNING_REQUIRED: "YES"
        ENABLE_HARDENED_RUNTIME: "YES"
      configs:
        Debug:
          CODE_SIGN_IDENTITY: "-"
          DEVELOPMENT_TEAM: ""
          CODE_SIGNING_REQUIRED: "NO"
          ENABLE_HARDENED_RUNTIME: "NO"
